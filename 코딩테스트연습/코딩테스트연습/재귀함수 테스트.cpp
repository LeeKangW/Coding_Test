#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int answer_cp = 0;

void make_queen(vector<int> q, int cnt)
{
	
}

int solution(int n)
{
	int answer = 0;
	vector<int> q(n);

	answer = answer_cp;
	return answer;
}